import React from 'react';

const Favourites = ({ recipe }) => {

  // ADD a way to add recipes to a favorites list (maybe use local storage for now, then add a database later)


  return (
    <div className="favourite-container">
      <h5>Favourite List with local storage is stored here! Also, add a history list? NO?</h5>
    </div>
  );

  
};

export default Favourites;